# TODO / Shortcuts (2h scope)

- Add unit tests for conversion logic and override rules.
- Debounce input to avoid too many history entries while typing.
- Improve number parsing for different locales.
- Persist last used values in localStorage.
- Split App.jsx into smaller components for better readability.
- Improve accessibility (aria labels, focus states).
- Better visual feedback when override is disabled automatically.
